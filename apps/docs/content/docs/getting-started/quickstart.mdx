---
title: Quickstart
description: From nothing to a public URL in front of your local server, and a first request through it.
verified:
  date: "2026-09-21"
  commit: 8c74574
  packages:
    "@vhyxvoid/agent": "1.0.20"
    "@vhyxvoid/sdk": "1.1.0"
sources:
  - packages/agent/src/cli.ts
  - packages/agent/src/AgentClient.ts
  - packages/sdk/src/client.ts
  - packages/sdk/package.json
  - apps/hub/src/handlers/HttpTunnel.handler.ts
  - apps/hub/src/router/Message.router.ts
  - apps/web/src/views/org/api-keys/CreateApiKeyDialog.tsx
---

By the end of this page you'll have a local server reachable at a public HTTPS URL and will have called it from code. You need [Node.js](https://nodejs.org) (the packages here were tested on Node 24) and something already listening on a local port. Any HTTP server works; the examples use port `3000`.

## 1. Create an account and an API key

1. Sign up in the <Dash to="/register">dashboard</Dash> and follow the verification email.
2. In the sidebar, open your organization and choose **API Keys**, then **Create API key**. The sidebar lists organizations only: if it shows just **Create organization**, create one first from the Dashboard (**New organization**). See [Accounts and organizations](/dashboard/account-and-organizations).
3. Give the key a name, pick the **DEV** environment, and keep the **`tunnel:connect`** scope selected. It is the only scope the agent needs.
4. Copy the **key ID** and the **secret** from the dialog that appears.

<Callout type="warn" title="The secret is shown once">
  VhyxVoid stores only a hash of it. If you lose it, rotate the key from the same page to get a new one.
</Callout>

<Callout>
  Production (**PROD**) keys need a paid plan. On the free plan, create a **DEV** key.
</Callout>

## 2. Run the agent

Run the interactive setup from your project directory:

```bash
npx @vhyxvoid/agent init
```

It asks for your key ID, the secret (shown as `*` as you type), the local port, a label, and the hub URL. Press Enter to accept the defaults for the label (`default`) and hub URL. It also asks for an account slug; leave that empty for now, because you'll see the slug in your tunnel URL in a moment.

Your answers are saved to `.env.vhyxvoid`, and that file is added to your `.gitignore` (created if you don't have one) so the secret doesn't get committed.

<Callout>
  Prefer to write the file yourself? Create `.env.vhyxvoid` containing `VHYXVOID_API_KEY=…`, `VHYXVOID_SECRET=…` and `VHYXVOID_PORT=3000`, and add it to `.gitignore`. Everything else has a default. The [CLI page](/integrations/standalone-cli) lists every setting.
</Callout>

Then start the tunnel:

```bash
npx @vhyxvoid/agent
```

When it connects you'll see:

```text
  ✅  Tunnel active

  Local:   http://localhost:3000
  Public:  https://acme--default.vhyxvoid.com

  Share the Public URL — it is stable and never changes.
```

The agent also prints a few status lines of its own, such as `[agent] authenticating with hub`, and a version banner before the tunnel comes up.

That `Public` line is your tunnel URL. It has the shape `https://<account-slug>--<label>.vhyxvoid.com`. The part before `--` is your account slug and the part after it is the label you chose.

<Callout type="info">
  If the agent connects but prints only `Local` and `Label` lines, it could not find an account slug for your key, so it has no public URL to show you. Check that the key belongs to the account you expect.
</Callout>

The agent keeps running until you stop it with Ctrl+C. If the connection drops it reconnects on its own, backing off up to five minutes between attempts.

## 3. Make a request through the tunnel

From any machine, use your own URL:

```bash
curl https://acme--default.vhyxvoid.com/
```

The request travels to the VhyxVoid hub, down the agent's connection, and to `http://127.0.0.1:3000/`. Your server's response, whatever it is, comes back the same way.

If it doesn't work, these are the responses VhyxVoid itself produces:

| Status | Meaning |
| --- | --- |
| `404` | No agent is registered for that URL. Start the agent, and check the slug and label. |
| `503` | The URL is registered but its agent is not connected right now. Restart the agent. |
| `413` | The request body was over 10 MB. |
| `502` | The agent is connected but your server didn't answer it: it isn't running on that port, or it took too long (agent 1.0.20 and later wait as long as the hub allows minus two seconds, 118 seconds on the default; 1.0.19 and earlier gave up after 28). |
| `504` | The agent didn't answer at all within the hub's timeout (120 seconds by default). Unusual. |

Anything else is your own server's response.

<Callout type="warn" title="Anyone with the URL can reach your server">
  The tunnel URL is public and carries no authentication. Only tunnel a server you're comfortable exposing, and protect sensitive routes in your own application. See [Limitations](/limitations).
</Callout>

## 4. Call it from code

To call the tunnel from a script, a CI job, or another server, install the SDK. It is a Node.js library (see [Limitations](/limitations#the-sdk-is-nodejs-only)):

```bash
npm install @vhyxvoid/sdk
```

```js title="call-tunnel.mjs"
import { createClient } from '@vhyxvoid/sdk'

const api = createClient({ accountSlug: 'acme', label: 'default' })

try {
  const { status, data } = await api.get('/')
  console.log(status, data)
} catch (err) {
  console.error(err.name, err.status ?? '', err.message)
}
```

Replace `acme` with the slug from your own URL. You can also set `VHYXVOID_ACCOUNT_SLUG` and `VHYXVOID_LABEL` in the environment and call `createClient()` with no arguments. In a CommonJS file use `const { createClient } = require('@vhyxvoid/sdk')` instead; both work.

`get`, `post`, `put`, `patch`, and `delete` return `{ data, status, headers, ok }`. JSON responses are parsed, text comes back as a string, and binary responses (images, PDFs) come back as a `Buffer`. A non-2xx response throws a `ClientError` carrying the `status` and the response `data`.

The HTTP client is a plain `fetch` to your tunnel URL and sends no VhyxVoid credentials. That is why it needs your slug but no key.

## A note on retries

Requests sent to the tunnel while the agent is reconnecting can fail with an error (`502`, `503` or `504`) even though your server may have handled them. Retrying a `GET` is safe. Retrying a `POST` or `DELETE` can apply the change twice, so make those handlers idempotent if a duplicate would hurt. Details are on the [Limitations](/limitations) page.

## Next steps

- Skip the separate process: start the tunnel from inside your app with the [Next.js](/integrations/nextjs), [Express](/integrations/express), or [Fastify](/integrations/fastify) integration.
- Every option of the `vhyxvoid` command: [CLI](/integrations/standalone-cli).
- Receive webhooks on your machine: [Webhooks](/integrations/webhooks).
- Read [Concepts](/getting-started/concepts) for how the pieces fit together.
