---
title: Limitations
description: What VhyxVoid does not do or guarantee, stated plainly.
verified:
  date: "2026-09-19"
  commit: a91d383
  packages:
    "@vhyxvoid/agent": "1.0.19"
    "@vhyxvoid/sdk": "1.0.1"
    "@vhyxvoid/middleware": "1.0.4"
    "@vhyxvoid/next": "1.0.4"
sources:
  - apps/hub/src/handlers/HttpTunnel.handler.ts
  - apps/hub/src/registry/Pending.registry.ts
  - packages/agent/src/replay/replayQueue.ts
  - packages/sdk/src/client.ts
  - packages/sdk/src/TunnelClient.ts
  - packages/agent/src/cache/ResponseCache.ts
  - packages/agent/src/proxy/BackendProxy.ts
  - packages/middleware/package.json
  - packages/next/package.json
---

## Requests don't survive agent disconnects

If your agent loses its connection to VhyxVoid and a request arrives before it reconnects, that request is not held and replayed afterwards. The caller gets a `504` after the 30 second timeout.

- **`GET` and other safe requests:** retrying is fine.
- **`POST`, `PUT`, `PATCH`, `DELETE`:** a request that was already handed to your server can still be processed after the caller has been told it failed. Retrying may apply the change twice. Make these handlers idempotent (for example with an idempotency key) if a duplicate would cause harm.

## Tunnel URLs are public

Anyone who knows or guesses a tunnel URL can send requests to your local server. VhyxVoid does not authenticate callers on the tunnel URL, and the SDK's HTTP client sends no credentials. Don't tunnel a server that holds real data, and protect anything sensitive in your own application.

## Cached responses

The agent keeps some `GET` responses in memory and can answer repeat requests without asking your server. It does this only when your server's response is a `2xx` with a `Cache-Control` header whose `max-age` is above zero, and it marks responses with an `x-vhyxvoid-cache: HIT` or `MISS` header.

- **Per caller.** Entries are keyed by path and query string and by the caller's credentials: cookies, `Authorization`, and headers whose names look like credentials (`x-api-key`, `x-auth-token`, `x-csrf-token`, `x-session-id`, and similar). Two callers with different credentials never share an entry, and an anonymous response is never given to a credentialed caller.
- **Never cached:** responses that set a cookie, or send `Vary: *`. A `Vary` header is honoured for the request headers it names.
- **What is not covered:** a custom credential header whose name matches none of those patterns. If your server authenticates that way, send `Vary` or `Cache-Control: no-store`.
- **Older agents.** In `@vhyxvoid/agent` 1.0.18 and earlier the cache ignored credentials, so a response produced for one user could be handed to another. Upgrade to 1.0.19 or later. The in-process integrations bundle the agent, so upgrade `@vhyxvoid/next` and `@vhyxvoid/middleware` to 1.0.4 or later too.

## Your server sees a local Host header

Requests reach your server with `Host` (and `x-forwarded-host`) set to `127.0.0.1:<port>`, not the public hostname, and with `x-forwarded-by: vhyxvoid`. Code that builds absolute URLs from those headers, or signs the request host, will not see your tunnel URL.

## Request size and time

- Request bodies are limited to **10 MB**. Larger requests get `413`.
- Your server has **30 seconds** to respond. After that the caller gets `504`.

## The WebSocket client exposes your secret to the browser

`TunnelClient`, the SDK's WebSocket client, is configured with your API key secret. If you use it in browser code, that secret is in your JavaScript bundle. If you must, use a dedicated DEV key with only the scopes it needs, and never a production key. The HTTP client (`createClient`) doesn't have this problem, because it uses no key.

## `@vhyxvoid/sdk` 1.0.1 has two known problems

These are bugs in the published SDK, not design limits. Neither is fixed yet, and the fixes for the agent and the framework integrations do not affect them. Each will be removed from this page when a fixed version is published.

### It can't be `import`ed

The package declares an ES module entry point that was never built, so `import … from '@vhyxvoid/sdk'` fails. Use `require()`, or `createRequire` from an ES module. See the [Quickstart](/getting-started/quickstart).

### It returns binary responses as garbled text

When the server answers with a binary content type (images, PDFs, audio, zip files), `createClient` decodes the body as UTF-8 text, which corrupts it: eight raw bytes came back as a string of replacement characters instead of a `Buffer`. The published 1.0.1 build predates the fix that exists in the source. Fetch binary responses with plain `fetch` against your tunnel URL until a new SDK version is released. JSON and text responses are fine.
