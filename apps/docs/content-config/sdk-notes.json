{
  "_comment": "Overrides and notes for the generated SDK reference. `descriptions` replace a JSDoc comment that is wrong, internal, or missing; `notes` are appended under an entry. Keys are `Symbol` or `Symbol.member`. scripts/generate.mjs fails if a key no longer matches an exported symbol or member, or if a JSDoc that mentions an internal note has no override. The source JSDoc for ClientConfig.accountSlug, TunnelClientConfig.hubUrl/timeout/localDiscovery and TunnelResponse.body was corrected 2026-09-25, so those overrides were removed.",
  "descriptions": {
    "RequestOptions.headers": "Extra headers for this request.",
    "RequestOptions.query": "Query string, without the leading `?`.",
    "RequestOptions.timeout": "Timeout for this request in milliseconds. Overrides the client's.",
    "ClientConfig.label": "Which tunnel to call: the part of the tunnel URL after `--`. Default: `default`. Env: `VHYXVOID_LABEL`.",
    "ClientConfig.baseUrl": "Use this URL instead of building one from `accountSlug`, `label` and `hubDomain`. Handy for tests.",
    "ClientConfig.hubDomain": "Domain the tunnel URL is built from. Default: `vhyxvoid.com`. Env: `VHYXVOID_HUB_DOMAIN`.",
    "ClientConfig.headers": "Headers added to every request.",
    "ClientConfig.timeout": "Request timeout in milliseconds. Default: `30000`.",
    "ClientConfig.onRequest": "Called before every request, with the request that is about to be sent. It can change `req.headers`, for example to add a token."
  },
  "notes": {
    "TunnelClientConfig.timeout": "The published `@vhyxvoid/sdk` 1.1.0 defaults to 30000 (30 seconds). The source has moved to 120000, in step with the hub's default request timeout, and that applies from the next SDK release.",
    "ClientConfig.timeout": "This is the client's own limit, measured by your process, and it is separate from the hub's request timeout (see [Tunnel request timeout](/reference/environment-variables#tunnel-request-timeout)). A slow route fails at 30 seconds here even if the hub would still be waiting, so raise it for slow endpoints.",
    "TunnelClient": "Node.js only, and it takes your API key secret. See [Limitations](/limitations#the-sdk-is-nodejs-only)."
  }
}
